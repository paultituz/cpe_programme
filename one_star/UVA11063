#include <iostream>
#include <vector>
#include <algorithm>
#include <sstream>
#include <ctype.h>
using namespace std;

int main()
{

    int num;
    int ca=1;
    while(cin>>num)
    {
       vector<int> save;
       vector<int> temp;
       int  ans=-1;

       for(int i = 0;i<num;i++)
       {
           int a;
           cin>>a;
           save.push_back(a);
       }

       for(int i=0;i<num-1;i++)
       {
           for(int j = i;j<num;j++)
           {
               int b = save[i]+save[j];
               temp.push_back(b);
           }
       }

       sort(temp.begin(),temp.end());
       unique(temp.begin(),temp.end());

       /*for(int i = 0;i<temp.size();i++)
       {
           cout<<temp[i]<<endl;
       }*/

       for(int i = 1;i<temp.size();i++)
       {
           if(temp[i-1]>=temp[i])
           {
               ans = 1;
               break;
           }
           else if (temp[i-1]<temp[i])
           {
               ans=0;
           }
       }

       if(ans==1)
       {
           cout<<"Case #"<<ca<<": It is not a B2-Sequence."<<endl;
       }
       else if(ans==0)
        {
           cout<<"Case #"<<ca<<": It is a B2-Sequence."<<endl;

        }


       ca++;
    }

    return 0;
}
